# Config file for automatic testing at travis-ci.org

language: python

python:
  - "3.4"
  - "2.7"
  - "pypy"

before_install:
  - pip install coveralls

install: 
  - pip install .

script:
  - py.test devops_utils

after_success:
  - coveralls
